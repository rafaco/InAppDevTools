
buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {

        //#ifdef ANDROIDX  //TODO: Gradle pre-processor: manual toggle comment is needed
        classpath 'com.android.tools.build:gradle:3.4.1'
        //#else
        //classpath 'com.android.tools.build:gradle:3.0.0'
        //#endif

        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.8.4'
        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1'
        classpath 'wang.dannyhe.tools:plugin:0.0.7'
    }
}

plugins {
    // Declare our plugin (make it available at subprojects)
    id "es.rafaco.inappdevtools" version "0.0.14" apply false
    id "org.sonarqube" version "2.7"
}

allprojects {
    repositories {
        mavenLocal()
        maven { url "https://jitpack.io"}
        google()
        jcenter()
    }
}

ext {
    pluginVersionName = '0.0.14'

    libraryPublishedVersion = '0.0.51'
    libraryWorkingVersion = '0.0.51'
    libraryVersionCode = 1
    libraryMinSdkVersion = 16

    appVersionName = '0.0.01'
    appVersionCode = 45
    appMinSdkVersion = 16

    //#ifdef ANDROIDX  //TODO: Gradle pre-processor: manual toggle comment is needed
    appTargetSdkVersion = 28
    appCompileSdkVersion = 28
    appBuildToolsVersion = '28.0.3'
    libraryTargetSdkVersion = 28
    libraryCompileSdkVersion = 28
    libraryBuildToolsVersion = '28.0.3'

    //TEMP without preprocessor
    //TODO: we don't need preprocessor, migrate to properties per variant
    libraryAndroidxMaterialVersion = "1.0.0-rc01"
    libraryAndroidxVersion = "1.0.0"
    libraryAndroidx2Version = "2.0.0"
    librarySupportVersion = '28.0.0'
    librarySupportArchVersion = "1.1.0"
    librarySupportPagingVersion = "1.0.1"

    //#else
//    appTargetSdkVersion = 26
//    appCompileSdkVersion = 26
//    appBuildToolsVersion = '26.0.2'
//    appSupportVersion = '26.0.2'
//    libraryTargetSdkVersion = 27
//    libraryCompileSdkVersion = 27
//    libraryBuildToolsVersion = '27.0.3'
//    librarySupportVersion = '27.1.1'
    //#endif

    /*WIP: API 23 -> gradleToolVersion = '2.1.0'
    appTargetSdkVersion = 23
    appCompileSdkVersion = 23
    appBuildToolsVersion = '23.0.2'
    appSupportVersion = '23.0.2'
    */
}



// Clean all modules
task clean(type: Delete) {
    delete rootProject.buildDir
}
subprojects {
    rootProject.clean.dependsOn tasks.matching { it.name == 'clean' }
}


sonarqube {
    properties {
        property "sonar.projectKey", "rafaco_InAppDevTools"
        property "sonar.organization", "rafaco-github"
        property "sonar.host.url", "https://sonarcloud.io"
        property "sonar.login", System.getenv('SONAR_TOKEN')
    }
}

//Enable Gradle report using param --scan
if (hasProperty('buildScan')) {
    buildScan {
        termsOfServiceUrl = 'https://gradle.com/terms-of-service'
        termsOfServiceAgree = 'yes'
    }
}


//TODO: Re-enable JavaDoc generation after common-logging removed.
subprojects {
    tasks.withType(Javadoc).all { enabled = false }
}

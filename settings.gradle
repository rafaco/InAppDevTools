pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}
enableFeaturePreview('STABLE_PUBLISHING')


//Include modules by folder name
include ':library', ':plugin', ':sample'

//Rename modules to correct artifactIds
rootProject.name = 'inappdevtools-project'
rootProject.children.each {
    it.name = ('plugin' == it.name ? 'inappdevtools-plugin' :
               'library' == it.name ? 'inappdevtools' :
               it.name)
}
